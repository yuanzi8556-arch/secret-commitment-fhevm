[build]
  command = "pnpm install && pnpm sdk:build && pnpm --filter nextjs-showcase build"
  publish = "packages/nextjs-showcase/.next"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
