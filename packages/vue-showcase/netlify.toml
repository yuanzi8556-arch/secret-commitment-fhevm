[build]
  command = "pnpm install && pnpm sdk:build && pnpm --filter vue-showcase build"
  publish = "packages/vue-showcase/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
